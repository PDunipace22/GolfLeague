@model IEnumerable<GolfLeagueApp.Models.MaxScore>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaxScoreType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaxScoreType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaxScoreID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaxScoreID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaxScoreID })
        </td>
    </tr>
}

</table>
